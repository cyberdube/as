import { AppendGmailPipe } from './append-gmail.pipe';

describe('AppendGmailPipe', () => {
  it('create an instance', () => {
    const pipe = new AppendGmailPipe();
    expect(pipe).toBeTruthy();
  });
});
